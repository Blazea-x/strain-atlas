name: PRODUCTION START V1

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

jobs:
  launch:
    name: Launch master-migration PRODUCTION START V1
    if: github.head_ref == 'ops/production-next5-20260819-1846'
    permissions:
      contents: write
      actions: read
    uses: Blazea-x/strain-atlas/.github/workflows/production-start-v1.yml@master-migration
    with:
      batch_size: "5"
      generate_images: true
    secrets: inherit
