name: PRODUCTION NEXT5 ONE-SHOT

on:
  pull_request:
    branches:
      - main
    types:
      - opened

jobs:
  launch:
    if: github.head_ref == 'ops/production-next5-20260819-1846'
    permissions:
      contents: write
    uses: Blazea-x/strain-atlas/.github/workflows/production-start-v1.yml@master-migration
    with:
      batch_size: "5"
      generate_images: true
    secrets: inherit
