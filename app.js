const esc=s=>String(s).replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;"}[c]));const rows=a=>a.map(([k,v])=>`<div class="label">${esc(k)}</div><div class="value">${esc(v)}</div>`).join("");const cards=document.getElementById("cards");cards.innerHTML=STRAINS.map(s=>`<details class="card" data-search="${esc(s.search.toLowerCase())}"><summary><div class="visual"><img src="${esc(s.appearance)}" alt="${esc(s.name)} appearance" loading="lazy"></div><div class="body"><h3>${esc(s.name)}</h3><div class="jp">${esc(s.jp)}</div><span class="type">${esc(s.type)}</span>${rows(s.summary)}<div class="tap"><span>タップして詳細を見る</span><span class="chev">⌄</span></div></div></summary><div class="details">${s.gallery.length?`<div class="gallery">${s.gallery.map((src,i)=>`<img src="${esc(src)}" alt="${esc(s.name)} image ${i+2}" loading="lazy">`).join("")}</div>`:""}${rows(s.details)}${s.reviews.map(r=>`<div class="review"><div class="review-title">${esc(r.title||"REVIEW")}</div><div class="review-body">${esc(r.body||"")}</div></div>`).join("")}</div></details>`).join("");document.getElementById("count").textContent=`${STRAINS.length} CULTIVARS`;document.getElementById("listed-count").textContent=`${STRAINS.length}件`;function filterCards(){const q=document.getElementById("q").value.trim().toLowerCase();document.querySelectorAll(".card").forEach(c=>c.style.display=!q||c.dataset.search.includes(q)?"block":"none")}document.querySelectorAll(".card").forEach(card=>card.addEventListener("toggle",()=>{if(card.open)document.querySelectorAll(".card[open]").forEach(other=>{if(other!==card)other.open=false})}));document.getElementById("q").addEventListener("input",filterCards);document.getElementById("search-btn").addEventListener("click",filterCards);